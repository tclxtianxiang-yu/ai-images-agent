{
  "name": "ai-images-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:worker": "opennextjs-cloudflare build",
    "start": "next start",
    "lint": "eslint",
    "deploy": "pnpm run build && pnpm run build:worker && wrangler deploy",
    "deploy:dev": "pnpm run build && pnpm run build:worker && wrangler deploy --env development",
    "deploy:prod": "pnpm run build && pnpm run build:worker && wrangler deploy --env production",
    "cf:dev": "wrangler dev",
    "cf:tail": "wrangler tail",
    "preview": "opennextjs-cloudflare preview"
  },
  "dependencies": {
    "@mastra/core": "^0.24.0",
    "@mastra/libsql": "^0.16.2",
    "@mastra/loggers": "^0.10.19",
    "@mastra/memory": "^0.15.11",
    "@opennextjs/cloudflare": "^1.11.1",
    "mastra": "^0.18.0",
    "next": "15.1.6",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.1.6",
    "tailwindcss": "^4",
    "typescript": "^5",
    "wrangler": "^4.46.0"
  }
}
